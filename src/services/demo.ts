import data from './../dummy.json';
import {supp,jubb} from './../types/demoTypes'

export const sqrt =()=> {
    let x = data.map((obj:supp)=>{
        
        console.log([obj])
        return obj
    })
    return x 
}
export const add = ()=>{
    let m = 89
    console.log(m)
}